# 主动回复判断跳过 QQ 号

## 已确认需求

- 在 TUI 的“主动回复判断”菜单中增加“跳过主动判断的 QQ 号”。
- 名单必须持久化，程序重启后仍然生效。
- TUI 必须能够查看、添加、编辑和删除名单项。
- 提供两个 AI 自主调用工具，分别按 QQ 号添加和删除名单项。
- 两个工具仅允许 Miyu 管理员使用，包括静态配置管理员和通过现有权限机制授予的动态管理员。
- 多个机器人 QQ 账号共用同一份名单。
- 命中名单时只跳过主动回复的社交判断；违规和安全判断继续执行。
- 不修改 `manage_platform_access` 的现有存储和对外行为。

## 设计结论

名单只有一个数据源：现有 SQLite `platform_plugin_kv` 表。TUI、主动判断和 AI 工具都通过同一个领域模块访问该名单，不在 `config.jsonc` 中再保存一份副本。

这样可以避免以下问题：

- AI 工具运行时正处于 active turn，直接改 `config.jsonc` 会绕过现有完整配置更新约束。
- 守护进程同时持有 Actor 配置和 Manager 配置，只更新其中一份会造成运行时不一致。
- TUI 保存完整配置快照，可能覆盖 AI 工具刚写入的名单。
- 静态配置名单与动态数据库名单并存会产生两个来源和不可预期的删除语义。

SQLite 方案使用现有事务型 `plugin_update_json`，无需新数据库表或配置重载。

## 数据模型

- 插件 ID：`real_context`
- 平台：`onebot`
- 账号：`*`
- 会话类型：`global`
- 会话 ID：`*`
- 键：`active_judgement_skip_user_ids`
- 值：排序、去重后的正整数 QQ 号集合

使用全局作用域，使所有机器人 QQ 账号和所有人格共享名单。空集合删除对应 KV 记录。

领域模块负责：

- 构造固定全局 scope。
- 读取并校验名单。
- 判断给定 QQ 号是否在名单中。
- 原子添加、删除和应用 TUI 差异。
- QQ 号必须是 `i64` 范围内的正整数。
- 限制最大名单数量，防止无界状态增长。

## TUI 行为

- 在“主动回复判断”菜单显示“跳过主动判断的 QQ 号：N”。
- 进入后复用现有 QQ 号列表编辑交互。
- 打开编辑器时读取当前数据库名单。
- 离开列表编辑器时计算用户操作产生的添加集和删除集，并在一个 SQLite 事务中应用到最新名单。
- 通过差异合并保留编辑期间由 AI 工具添加或删除的无关 QQ 号，避免整表快照覆盖。
- 该名单属于持久运行状态，因此离开名单编辑器时立即提交，不依赖主配置页面的“保存并退出”。

## AI 工具

新增两个写工具：

- `add_active_judgement_skip_qq`
- `remove_active_judgement_skip_qq`

共同约束：

- 只在 OneBot real_context 插件启用且当前请求者是有效 Miyu 管理员时注册。
- handler 执行时再次检查当前静态或动态管理员权限，不能只信任 turn 创建时的 `is_admin` 快照。
- 参数只接受一个必填 `qq_id`，字符串或整数均可，规范化为正整数。
- 添加已存在项、删除不存在项均为成功的幂等操作，并返回明确结果。
- 标记为 `Writes`。
- Rust host 发送唯一、固定的 QQ 结果消息，并抑制模型追加确认回复，沿用 `manage_platform_access` 的输出模式。
- 修改成功后立即影响后续主动判断；不取消已经开始的判断。

## 主动判断语义

在 real_context 群消息触发判断入口读取名单：

- 未命中：保持现有行为。
- 命中且是普通群消息：不进入概率、续聊或覆盖触发的主动回复判断。
- 命中且是直接触发：不由 real_context 接管判断，恢复原有直接触发回复。
- 命中但触发违规/安全候选：仍允许 moderation-only 判断，不生成由安全关键词意外引起的主动社交回复。
- 历史记录、上下文注入、好感度等其它 real_context 功能不受影响。

## 模块边界

- 新建 `real_context/active_judgement_skip.rs`，集中持久化、名单判断、工具注册和共享解析逻辑。
- `real_context/mod.rs` 只调用名单判断并注册工具，不包含数据库细节。
- `config_tui.rs` 通过领域模块读取和提交差异，不直接拼接数据库键。
- 管理员有效性检查优先复用或提取现有 access-control 逻辑，避免复制静态/动态管理员判断。

## 验证

- 名单读取、排序、去重、正整数校验和数量上限。
- 添加/重复添加、删除/重复删除、删除最后一项。
- StateStore 重开后名单仍存在。
- TUI 差异合并不会覆盖并发的无关修改。
- 多机器人账号读取同一个全局名单。
- 静态管理员、动态管理员和普通用户的工具可见性及 handler 二次鉴权。
- 两个工具均为写工具，参数验证正确，且只发送一条固定结果。
- 命中名单后跳过概率、续聊、覆盖和直接触发判断，同时保留 moderation-only 判断。
- 运行 `cargo fmt --check` 和相关 Rust 测试；根据结果补充全量 `cargo test`。

## 非目标

- 不迁移 `manage_platform_access`；仅允许提取管理员判断和固定输出等共享 helper。
- 不把名单写入 `config.jsonc`。
- 不增加第三个查询工具；查询和人工管理由 TUI 提供。
- 不取消名单变更前已经启动的主动判断任务。

## 实施结果

- 已完成全局 SQLite 名单、TUI 编辑入口、两个管理员工具和主动判断接入。
- 已将动态管理员鉴权与名单写入放在同一权限锁内，避免撤权竞态。
- 已修正固定工具输出的抑制条件：结果消息成功发送后，即使业务操作失败，也不会再追加模型回复。
- 名单读取或校验失败时采用 fail-closed：关闭社交主动判断，同时保留直接触发和 moderation-only。
- supersede 接管路径同样检查名单；命中或读取失败时回到正常判断链路，不取代进行中的生成。
- 主动判断热路径使用 250ms 内存缓存，并通过 SQLite `updated_at` 检测其它进程的名单更新；工具和 TUI 写入会立即失效本进程缓存。
- 通用 TUI 菜单已增加滚动视口，长 QQ 号列表始终保持选中项可见。
- 名单读取和提交错误会在主动判断菜单内显示，不再退出整个配置 TUI。
- `cargo check`、`cargo fmt --check` 和 `git diff --check` 通过。
- 跳过一项与本任务无关的 README token 固定向量后，主测试 1200 项、daemon reload 2 项、renderer worker 1 项全部通过。
- 未跳过时的全量测试仅失败一项：用户现有 `README.md` 修改使硬编码 token 期望值 2884 变为实际值 2956；本任务未修改该测试或 README。
