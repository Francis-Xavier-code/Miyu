- ~~拆分代码，提升代码的可维护性~~ **已完成**（分支 `refactor/split-maintainability`，
  55 次提交）。基准量自 18 个高质量 crate 的 1095 个文件：目标 800 / 上限 1500 /
  红线 2000。越红线文件 20→0，超上限 33→0，最大文件 16,842→1,035，中位数 327，
  P95 849，用例数 1490 全程未变。经过、工具与九类坑见
  `docs/fixed/2026-08-18-代码拆分.md`，
  新代码地图见 `docs/wiki/14-参与开发.md`。
  - 第二轮把 1000 行以上的 44 个文件也拆了，只剩 1 个（`cli/repl/remote/interactive.rs`
    整个文件就是 1021 行的 `run_remote_repl`）。最大文件 1,035 行，P95 849。
  - 未做：`impl` 内部的巨型函数（`chat_with_tools` 956 行、`run_remote_repl`
    1021 行）——拆开要抽中间状态、定义新类型，会改行为。
- 减少token消耗
- 生成图片后图片打印出来太大了并且被后续内容往上推的时候还会有错位bug，噢，看起来打印图片也有这个bug。天呐，我测试了一下打印图片功能全都有问题。
  - 「错位」见下方搁置项。
- **[搁置·待研究] kitty 图片/公式滚动残影**：打图后用鼠标滚上去看历史，AI 继续
  输出把内容顶上去时，图片底部切片会一条条向下重复渲染。公式在 kitty 路径下
  也是图片，同病。
  - 已用字节级录像排除：Miyu 发的占位符一个不多（2525 = 50×15×3 + 25×11），
    2349 次绝对定位全在 51–57 行，没碰过正文区；尺寸、协议用法、单元格检测、
    重复渲染路径均已排除。
  - 机制（kitty 图形协议原文）：设了页边距（DECSTBM）之后，只有**完全落在页
    内**的图片才跟着滚动，越界的会被裁剪并留在原地。Miyu 活动区固定在屏幕底
    部、正文用受限滚动区滚，图片迟早会碰到页上沿，之后就被钉住，文字从它身上
    流过去。
  - 已做但不够：图片打完立刻整屏上滚一次抬进页内（把"一打完就被钉住"推迟到
    "滚到顶部才被钉住"，推迟了没消除）。
  - 候选修法：正文里有图片时改用整屏滚（清空活动区→整屏滚→重画），纯文字回合
    保持零开销路径。评估过 alt-screen TUI 重写——能根治整类问题，但会拿掉原生
    scrollback，且与代码拆分冲突，不建议现在做。
  - 排查全记录：`docs/fixed/2026-08-17-终端图像与渲染.md` §八
  - 测具：`testkit/kitty-image/{probe,scroll_probe,scroll_variants}.py`
- `agent::tests::manual_persona_reminder_overrides_distillation` 偶发红：用例起了
  真实 TCP 服务器（`bind 127.0.0.1:0` + SSE），单独跑 8/8 全过，只在全量并行
  加压时失败。典型并发时序 flake。



- 做一个专门的dsh模式，专门给deepseek用，由于deepseek过拟合导致非deepseekharness极简模式的情况下性能大幅下降，所以要做一个dsh模式，两阶段插件，第一阶段使用deepseekharness极简模式一比一复刻，让DeepSeek进入优质思维链，然后从第二轮开始再开放全部工具。

- 工具描述信息用英文写，所有提示性语句（除了用户自己设置的prompt）也都用英文，用英文也许更简洁，更省token，AI也更好理解，更符合AI的训练场景。中文的提示会导致AI使用中文的思维链，而中文的思维链似乎不如英文的思维链高效。

- token统计和缓存统计正确吗？

- webui附件局域网可下载。AI可以把本地文件通过Webui分享出去，别人通过局域网访问webui，可以下载那个文件。

- 我觉得天气查询自动定位应该去掉。AI会不去传入城市，导致自动定位，但自动定位并不准。如果直接运行应当提示提供查询地址。

- 接入claude code，使用claude code的订阅

- 生图功能在通讯平台里加限制，每个会话每次请求只能生成一张图，不允许AI批量生图。管理员和私聊白名单用户不受限制

- 新QQ插件：定时发送消息。功能是配置会话、每天几点几分（支持多个时间点）、发送什么消息。然后到点了程序就会自动发消息。

  





- 重写完整TUI
- REPL 复制不带左侧装饰
- 优化占用，提高运行效率和稳定性
- 接入通讯平台的回复处理插件字体处理优化
- webui设置界面优化
- 插件dashboard移植
- 图片渲染不再依赖chafa

## Feats

- 权限管理
- MacOS适配
- 文转音频
- 语音输入
- Live2D
- 支持QQ官方机器人
- 支持telegram
- 安全性、权限
- artifact增强
- 自带skill扩充
- 通讯平台命令
