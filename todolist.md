## 细节优化

- 拆分代码，提升代码的可维护性
~~QQ消息如果是工具或者命令输出结果也应该走回复处理插件，例如/models这样的命令。~~ ✅ 08-17
~~latex 渲染问题~~ ✅ 08-17
- 减少token消耗
~~DSH在turn的每一步完成都会刷新统计，但是Miyu不会，footer总是在turn结束才更新。~~ ✅ 08-17
- `管理表情包`改成`表情包`
- 生成图片后图片打印出来太大了并且被后续内容往上推的时候还会有错位bug，噢，看起来打印图片也有这个bug。天呐，我测试了一下打印图片功能全都有问题。
  - ~~「太大」已修 ✅ 08-17~~：daemon 模式下尺寸在 CLI 侧丢了，只有 show_meme 算尺寸，
    其余一律铺满整个终端。
  - 「错位」见下方搁置项。
- **[搁置·待研究] kitty 图片/公式滚动残影**：打图后用鼠标滚上去看历史，AI 继续
  输出把内容顶上去时，图片底部切片会一条条向下重复渲染。公式在 kitty 路径下
  也是图片，同病。
  - 已用字节级录像排除：Miyu 发的占位符一个不多（2525 = 50×15×3 + 25×11），
    2349 次绝对定位全在 51–57 行，没碰过正文区；尺寸、协议用法、单元格检测、
    重复渲染路径均已排除。
  - 机制（kitty 图形协议原文）：设了页边距（DECSTBM）之后，只有**完全落在页
    内**的图片才跟着滚动，越界的会被裁剪并留在原地。Miyu 活动区固定在屏幕底
    部、正文用受限滚动区滚，图片迟早会碰到页上沿，之后就被钉住，文字从它身上
    流过去。
  - 已做但不够：图片打完立刻整屏上滚一次抬进页内（把"一打完就被钉住"推迟到
    "滚到顶部才被钉住"，推迟了没消除）。
  - 候选修法：正文里有图片时改用整屏滚（清空活动区→整屏滚→重画），纯文字回合
    保持零开销路径。评估过 alt-screen TUI 重写——能根治整类问题，但会拿掉原生
    scrollback，且与代码拆分冲突，不建议现在做。
  - 排查全记录：`docs/fixed/2026-08-17-终端图像与渲染.md` §八
  - 测具：`testkit/kitty-image/{probe,scroll_probe,scroll_variants}.py`
- footer 回合中途显示的上下文窗口是配置默认值（168k），回合结束才被权威值覆盖，
  数字跳一下。只在会话第一轮出现。
- `agent::tests::manual_persona_reminder_overrides_distillation` 偶发红：用例起了
  真实 TCP 服务器（`bind 127.0.0.1:0` + SSE），单独跑 8/8 全过，只在全量并行
  加压时失败。典型并发时序 flake。
- 子代理继承主智能体上下文
- 工具描述信息用英文写？
- token统计和缓存统计正确吗？
- REPL 复制不带左侧装饰
- 优化占用，提高运行效率和稳定性
- 接入通讯平台的回复处理插件字体处理优化
- webui设置界面优化
- 插件dashboard移植
- webui附件局域网可下载
- ctrl+左右方向键按词跳跃光标
- 进入普通模式REPL的时候不应该进如终端集成会话
- daemon运行在另一台电脑但是可以操作这台电脑
- 退出miyurepl后再进入不会显示出正确的footer 里的上下文信息，会是零，对话一次后才恢复正常。
- 当在一个repl输入内容，输入的内容会进入上键可以调出来的那个历史里，但是如果我再开一个repl，也是这个会话，上键无法调出刚刚输入的内容历史
- 图片渲染不再依赖chafa

## Feats

- 权限管理
- MacOS适配
- 文转音频
- 语音输入
- Live2D
- 支持QQ官方机器人
- 支持telegram
- 安全性、权限
- artifact增强
- 自带skill扩充
- 通讯平台命令
