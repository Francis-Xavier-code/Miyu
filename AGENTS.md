# 在 Miyu 仓库里干活

写给在这个仓库工作的 AI。**不是流程手册，是踩过的坑。** 每条都写了「为什么」
——理解了为什么，才知道什么时候该变通；照字面执行反而会出事。

- 设计理念的完整论证：`docs/理念.md`
- 历次排查的全过程：`docs/fixed/`
- 代码地图与目录结构：`docs/wiki/14-参与开发.md`

---

## 按任务查

| 我要做的事 | 先读 |
|---|---|
| 改 `agent/` `llm/` `tools/registry.rs` 或任何提示词 | [1.1 字节确定性](#11-消息组装路径必须逐字节确定) |
| 加一个工具 / 改工具描述 | [1.1](#11-消息组装路径必须逐字节确定)、[1.4](#14-别把取证链一起洗干净) |
| 加一层跨模块调用 | [1.2 依赖方向](#12-底层不许知道上层) |
| 查一个 bug | [第 2 节 排查方法](#2-排查问题) |
| 修完了想收工 | [2.3 修复必须能证伪](#23-修复必须能证伪) |
| 搬文件 / 拆模块 / 改目录 | [3.3 搬文件的七个坑](#33-搬文件的七个坑) |
| 提交前 | [3.1 改完跑什么](#31-改完跑什么) |
| 回复用户 | [第 4 节 协作](#4-和用户协作) |

---

## 1. 不许违反的

这一节的四条有个共同点：**违反了编译能过、测试也不报错**，问题要么在账单上
显现，要么在很久以后才炸。

### 1.1 消息组装路径必须逐字节确定

Miyu 的成本结构建立在供应商的前缀缓存上，命中的 token 只按十分之一计价。
缓存**不懂语义，只比字节**：从第一个不同的字节起，后面全部作废。

在 `agent/`、`llm/`、`tools/registry.rs`、提示词组装这些路径上：

- **只能往尾部追加**，不许在已有内容中间插入或删除。一个时间戳插进 system
  prompt 中段，能让整条链每轮全 miss。
- **不许引入非确定性**。HashMap 迭代序、`Instant::now()`、随机 ID——任何两次
  运行结果不同的东西进了组装路径，缓存就废了。
- **工具目录必须跨轮、跨会话逐字节一样**，否则从 token 0 开始重算。

> 这类退化没有症状：功能全对，只是账单翻十倍。
> 详见 `docs/理念.md` 第一、三章。

### 1.2 底层不许知道上层

| 这个模块 | 不许引用 |
|---|---|
| `llm` `state` `memory` `tools` | `web` `cli` `config_tui` `platforms` |
| `render` | `web` `cli` `platforms` |
| `agent` `platforms` | `web` `cli` `config_tui` |
| `web` | `cli` `config_tui` |

现存的违规（目前只剩 `tools → platforms` 3 处）写在
`scripts/arch-dep-waivers.json`，**只许减不许增**。

规则的真相源是 `scripts/arch_dep_check.py` 的 `FORBIDDEN`，
`bash scripts/refactor-check.sh` 会检查。

### 1.3 权限由代码承担，不靠提示词

不要在提示词里「授予」或「限制」权限——模型不是权限系统。真实鉴权在
`platforms/access_control.rs` 和各执行层。

### 1.4 别把取证链一起洗干净

落库刻意保留模型发来的**原始**工具参数，包括半截 JSON。它是定位根因的唯一
线索，顺手洗干净等于毁掉下次的证据。

修数据的收口（见 [2.4](#24-收口放唯一入口别按调用点打补丁)）放在使用侧，不要
放在落库侧。

---

## 2. 排查问题

### 2.1 报错先当嫌疑人，不当证词

同一天连踩三次，共性是**报错说的原因不是真原因**：

| 报错说 | 实际是 |
|---|---|
| `start_line is required` | 参数传了，只是写成字符串 `"1"`，`as_u64()` 返回 `None` |
| `Moegirlpedia page not found` | 页面存在、HTTP 200，只是正文超了字节上限 |
| 「图片打印得太大」 | 尺寸算得完全正确，是画到了预留行数之外 |

三次都是先信了报错，然后在错误的抽象层上排查。

**反过来也要做**：Miyu 的很多参数是模型填的，不是人填的。人填错会看到报错、
会改；模型填错只会看到一句误导的话，然后换个同样错的方式重试。所以写报错要
说自己真正知道的——「期望整数，收到字符串 `"1"`」比「必填项缺失」有用一个
数量级。

> 详见 `docs/理念.md` 第九章。

### 2.2 先查规范，再读代码，最后才轮到推理

有个终端图片渲染的问题猜了四轮都没中。第四轮之后去搜 kitty 图形协议原文，
一句话就说清了机制：「设了页边距之后，只有完全落在页内的图片才跟着滚动，
越界的会被裁剪并留在原地」。

四轮猜测的成本远高于一次查证。**涉及协议、格式、第三方行为时，先去找权威
描述。**

### 2.3 修复必须能证伪

**「修完之后现象没了」不构成验证。**

2026-08-17 栽过一次：某依赖库往 stderr 打两行噪声，我做了抑制，跑测试数
stderr 得到 0 行，宣布修好。但那个测试**不加抑制也是 0 行**——它渲染的公式全
是纯数学，而会说话的是 Unicode 回退字体，只有 `\text{中文}` 才会加载它。整个
验证没有区分能力，等于拿一个恒为绿的测试给自己发合格证。用户装上之后噪声照旧。

所以：

- 新加的回归用例，**退回修复前必须报红**。不报红的用例守不住任何东西。
- 前后对照的成本很低（把修复临时去掉再跑一遍），省掉它的代价是把没修好的
  东西当修好的发出去。
- 门禁脚本同样适用：`scripts/` 下每道门禁都造过假回归验证它抓得住。

### 2.4 收口放唯一入口，别按调用点打补丁

同一类问题一天踩三次，前两次都在补调用点，每次都漏：补完主循环漏了子代理，
补完落库和历史回放漏了回合内的活体路径，补完字符串数组漏了对象数组。

**调用点会增长，补丁跟不上。** 收口要放在类型的构造函数、注册表的分发处这种
唯一入口上。现有两个例子：

- `ChatMessage::assistant`——tool_calls 进入消息的唯一入口
- `ToolRegistry` 的分发处——按工具自己的 schema 还原被写坏的参数

---

## 3. 动手改代码

### 3.1 改完跑什么

```bash
bash scripts/refactor-check.sh     # 五道门禁，任何一道红就别提交
```

| 门禁 | 挡什么 |
|---|---|
| 格式 | 改动过的文件违规数不得增加（存量不追） |
| 编译 | `--all-targets`，测试代码也要编过 |
| 测试 | 全量通过，且**用例数不得减少**——搬测试最容易整个 mod 漏掉 |
| 文件规模 | 不得新增越线文件；超标文件不得再长（10 行容差） |
| 依赖方向 | 见 [1.2](#12-底层不许知道上层) |

改了消息组装路径的，额外手测两轮请求的 cache log
（`cache-usage.*.jsonl`），确认第二轮的 `cache_read` 没有异常下降。

### 3.2 文件规模基准

**量出来的，不是拍的**：18 个公认高质量 crate 的 1095 个源文件，中位数 183 /
P90 935 / P95 1412。于是：

| 线 | 行数 | 含义 |
|---|---|---|
| 目标 | 800 | 希望值，超了不拦 |
| 上限 | 1500 | 硬约束 |
| 红线 | 2000 | 立刻必须处理 |

推导过程见 `scripts/refactor_size_report.py` 的文档注释。

### 3.3 搬文件的七个坑

拆模块时踩全了。共同点：**大多不报错，只是悄悄搬错或少搬**。

| # | 坑 | 症状 |
|---|---|---|
| 1 | `include_!` 相对当前文件 | 静默指到另一个目录 |
| 2 | `super::` 含义变了 | 静默指到另一个模块 |
| 3 | 子模块名遮蔽兄弟模块 | 报一堆无关的「找不到」 |
| 4 | 裸字符串骗过数括号的脚本 | 静默少搬 |
| 5 | 括号计数规则 | 静默截断或丢弃 |
| 6 | 新文件与已有文件同名 | 静默覆盖，整个 mod 消失 |
| 7 | 导入顺序 / 残留空行 | 格式门禁报红 |

**1. `include_!` 是相对当前文件的，不是相对 crate 根。**
`web.rs` 里有 27 处 `include_str!("../web/vendor/...")` 指向仓库根的前端资源。
一旦变成 `src/web/mod.rs`，`../web/` 就解析到 `src/web/` **自己**——不报错，
只是悄悄指错。必须同时改成 `../../web/`。同类的还有 `skills.rs`、
`token_estimate.rs`、`tools/task.rs`、`tools/package_advisor.rs`。

**2. `super::` 的含义会变。**
代码从 `x.rs` 搬进 `x/sub.rs` 之后，`super::` 从「x 的父模块」变成「x 自己」。
大多数时候不报错，只是悄悄指到另一个模块。`pub(super)` 同理——可见性范围跟着
缩了一层，上层就取不到了。搬完一律改写成绝对路径；`super::super::` 当心被
替换两次。

**3. 子模块名不要和上层的兄弟模块同名。**
踩过三次，报错和遮蔽毫无关系：

| 新模块 | 遮蔽了 | 报出来的错 |
|---|---|---|
| `web::ipc` | `crate::ipc` | 58 条「找不到函数 `send`」 |
| `onebot::commands` | `platforms::commands` | 找不到 `ParsedPlatformCommand` |
| `agent::vision` | `tools::vision` | 找不到 `register_scoped_platform` |

改个名就好（`ipc_server`、`builtin_commands`、`describe`），但不知道这条时会
往完全错误的方向查。

**4. 裸字符串会骗过按行做正则的脚本。**
`r#"{"a": 1}"#` 里的引号和花括号都不是代码，但正则会把 `"{"` 当成一个完整
字符串，剩下的花括号漏进计数。`openai_compatible.rs` 有 73 处裸字符串，
`mod tests` 的结尾因此定到第 6163 行而不是 8629——**不报错，只是少搬了三分之
二**（724 行 vs 3190 行）。裸字符串和块注释还能跨行，逐行独立扫描一定错。
统一用 `scripts/rustscan.py`。

**5. 括号计数：三种括号都配平，但只有 `{` 算「条目开始」。**
三次踩空，方向各不相同：

| 错误规则 | 反例 | 后果 |
|---|---|---|
| 只数 `{}` | `const T: [S; N] = [S { .. }, ..];` | 第一个元素处就截断 |
| `[` 也算开始 | `fn f<W>(p: &[u8]) -> R` | 第一行就归零，函数体连同 `where` 被丢下 |
| `(` 也算开始 | 带 `where` 的泛型函数 | 签名的 `)` 处归零，同上 |

没有花括号的条目（数组常量、类型别名）靠「深度归零 + 行尾分号」收尾。
见 `scripts/rustscan.py`。

**6. 新文件别和已有文件同名。** 拆分脚本写目标文件时如果目标已存在，会把它
整个覆盖掉——**编译照样过，只有「用例数不得减少」那道门禁看得见**。
`onebot/tests/delivery.rs` 就这么被冲掉过一次，8 个测试凭空消失。
`scripts/resplit_tests.py` 现在会拒绝覆盖已存在的文件。

**7. 导入顺序与残留空行。**
原地把 `crate::web::` 换成 `crate::runtime::` 之后那一行就不在字母序上了，
格式门禁会报。用 `scripts/fmt_imports.py` 只重排开头的 `use` 块——全文件跑
`cargo fmt` 会顺手改掉一堆历史遗留排版，产生与本次改动无关的巨大 diff。
抽走代码块留下的连续空行也要收掉。

> 拆分用的脚本清单与用法见 `docs/fixed/2026-08-18-代码拆分.md` 第四节。

### 3.4 写代码

- **注释写为什么，不写是什么。** `// 加一` 是噪声；「这里不能用受限滚动区，
  因为 kitty 只搬完全落在页内的图片」是资产。踩过的坑尤其要写下来，**包括
  试过但不对的方案**——省下的是下一个人重走一遍的时间。
- **每个模块文件开头写一段 `//!`**，说清它为什么单独存在。这是别人找东西时
  第一眼看的。
- **匹配周围代码的风格**：注释密度、命名、惯用法。这个仓库的注释是中文的。
- **有专用工具就别用 shell**：读文件用 Read 不用 `cat`，改文件用 Edit 不用
  `sed`。

### 3.5 改结构时的默认约束

除非用户明说要改行为，**结构调整一律零行为变化**：纯移动 + 可见性调整，不顺
手改业务逻辑、不修 bug、不做优化。想修的东西记下来单独提，混在一起会让
「这次改动到底动了什么」无法回答。

---

## 4. 和用户协作

- **中文回复。**
- **先报告后执行**：动手前说清楚要做什么，尤其是不可逆或影响面大的改动。
- **要真实实测 + 数据表格**，不要「应该没问题」。这个仓库的每个结论都该有
  实测背书。
- **不确定就问**，不要自己拍板然后把猜测当结论讲出来。
- **做不到的部分要明说**，不要悄悄缩小范围。
- **提交时机由用户决定**；诊断阶段的中间产物不要提交。
