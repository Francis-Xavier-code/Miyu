{
  "name": "task",
  "display_name": "子代理",
  "description": "启动子代理独立处理复杂任务，工具集与你相同。长任务可用 background=true 后台运行，完成后自动唤起。",
  "parameters": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "简短任务描述，用于进度展示。"
      },
      "prompt": {
        "type": "string",
        "description": "详细任务提示。必须包含完整上下文、目标和输出要求，子代理看不到你的对话历史。"
      },
      "max_steps": {
        "type": "integer",
        "description": "可选工具调用步数上限。默认不限，任务完成即结束。"
      },
      "tier": {
        "type": "string",
        "enum": [
          "cheap",
          "balanced",
          "strong"
        ],
        "description": "可选模型档位：cheap 简单查询，balanced 常规多步，strong 高难推理。默认 balanced，未配置的档位回退主模型。"
      },
      "background": {
        "type": "boolean",
        "description": "后台分离运行，立即返回 job_id，用 job(action=status) 查进度，完成后自动唤起你。"
      },
      "resume_id": {
        "type": "string",
        "description": "可选。上一次 task 因连接中断失败并在错误里给出 resume_id 时携带它，可从最后一个已完成的工具轮继续（仅本进程有效）。"
      }
    },
    "required": [
      "description",
      "prompt"
    ]
  },
  "permission": "writes",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "agent"
  ],
  "timeout_seconds": 0
}
